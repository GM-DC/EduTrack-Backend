package com.owlcode.microsaap.features.clase.domain.model

enum class ClassMode { PRESENTIAL, VIRTUAL, HYBRID }
enum class RecurrenceType { NONE, DAILY, WEEKLY, MONTHLY }
enum class RecurrenceEndType { NEVER, UNTIL_DATE, COUNT }
